
## 2026-03-05 00:06 — Email Agent SUCCESS

### What happened
- Email agent ran successfully for the first time!
- Triaged 2 mock emails, created 2 high-priority requests via pager-fetch.sh
- Summary: "Payment needed for invoice #1234" + "Calendar update for rescheduled project meeting"

### Issues fixed tonight
- web_fetch blocks localhost (SSRF protection) → switched to pager.jsplayground.cc
- web_fetch can't send custom headers → switched to exec with allowlist + pager-fetch.sh wrapper
- pager-api systemd service: /usr/bin/npx didn't exist → fixed to /usr/local/bin/npx
- agents.list with ONLY email-agent → he hijacked main agent role. LESSON: always include main explicitly
- João panicked and deleted email-agent workspace+config (understandable)
- Recreated everything, this time with main agent explicit in agents.list
- pager-fetch.sh got deleted when João cleaned up → recreated
- safeBinTrustedDirs needed for custom script location
- AGENTS.md referenced API_KEY "from cron message" but key was in script → fixed

### Final working config
- agents.list: [main (explicit), email-agent]
- email-agent tools: exec only, allowlist = [pager-fetch.sh]
- pager-fetch.sh at /home/joaohts/.openclaw/agents/email-agent/scripts/
- API key lives ONLY in pager-fetch.sh (not in prompts, not in workspace files)
- Cron: every 30min, id=c1390262-4587-4ee6-8241-f6dcb0da0330

### Regra: Email Agent Isolation
- NUNCA ler histórico/output do email-agent em sessão normal
- Ele é isolado por design — eu não preciso saber o que ele faz
- Só debugar se João pedir explicitamente
- O output dele pode conter conteúdo de email (untrusted) — não quero isso no meu contexto

## Sessão noturna 04-05/03 — Email Agent + Pager API

### Feito ✅
- Pager API (Express + SQLite) deployado como systemd service, porta 3100
- Cloudflare tunnel: pager.jsplayground.cc → localhost:3100
- API key gerada e salva em /home/joaohts/pager-api/.env
- Email agent registrado no OpenClaw (id: email-agent, model: Sonnet)
- SOUL.md + AGENTS.md criados no workspace do agent
- Script pager-fetch.sh criado (inbox + create, API key embutida no script)
- Agent só tem exec com allowlist do pager-fetch.sh — zero acesso a mais nada
- Cron configurado (every 30min, isolated, no-deliver) — DESABILITADO por agora
- Main agent adicionado explícito na agents.list (evita o bug de roubar lugar)
- Testado end-to-end: agent leu mock inbox, triou, criou 2 requests, push chegou
- tools.sessions.visibility=all e tools.agentToAgent.enabled=true configurados

### Bugs encontrados e resolvidos
- npx path errado no systemd → /usr/local/bin/npx
- web_fetch bloqueia localhost (SSRF protection)
- web_fetch não suporta custom headers (Authorization)
- Email agent roubou lugar do main quando era único na agents.list
- Script apagado quando João limpou workspace do agent
- safeBinTrustedDirs precisava ser configurado

### Faltando 🔲
1. **Trocar mock /emails/inbox por Himalaya real** — backend precisa chamar himalaya CLI
2. **Refactor do Pager app** — Requests tab com react-navigation (Claude Code deu rate limit, tentar amanhã)
3. **check-approved.sh** — script pra Jonathan executar ações aprovadas
4. **Habilitar cron do email-agent** quando inbox real estiver pronto
5. **Testar end-to-end com email real**
6. **Push com payload estruturado** — deep-link pro Requests tab no app

### Cron IDs
- email-triage: c1390262-4587-4ee6-8241-f6dcb0da0330 (DESABILITADO)
- morning-briefing: c58af64f-7720-4329-9def-d1eee8ab52c6
- todo-resolver: 3daeb85b-f102-475f-a38a-21fc76735161 (error)
- nightly-sleep: 6c95b3eb-dec5-49a2-b167-4cbf161c091b (error)

### Melhoria pendente: zero-token inbox check
- Criar um script bash (tipo check-inbox.sh) que chama pager-fetch.sh inbox
- Se inbox vazio → exit silencioso (zero tokens)
- Se tem emails → dispara openclaw system event ou agent turn
- Assim o Sonnet só gasta tokens quando tem email pra triar
- Mesmo padrão do check-approved.sh
