
## Session ~00:00-00:24 BRT

### Reminder Skill
- Created and tested reminder skill at `skills/reminder/`
- First attempt with `delivery: announce` failed — main session gave NO_REPLY and swallowed the message
- Fix: `delivery: none` + agentTurn prompt tells isolated agent to use `message` tool directly
- Tested "batata" successfully at 00:07
- Updated 8am Danilo reminder (job `b72a8d26`) to use new pattern
- Skill updated to reflect correct pattern

### Informal Calendar
- Created `jonathan/calendar.md` in Obsidian vault
- Daily cron `daily-calendar-summary` (job `c58af64f`) runs at 8 AM BRT
- Agent reads calendar, sends WhatsApp summary, cleans past events
- Uses same robust pattern: isolated agentTurn + delivery:none + message tool

### Key Learning
- `delivery: announce` is unreliable for notifications — depends on main session behavior
- `delivery: none` + agent sends via `message` tool = fully independent, robust pattern
- This is now the standard for all reminder/notification cron jobs
