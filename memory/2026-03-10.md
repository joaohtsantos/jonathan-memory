# 2026-03-10

## Spotify Backup Tool — DONE ✅
- Built with Claude Code CLI (Opus 4.6), project at `/home/joaohts/spotify-backup/`
- TypeScript + tsx, OAuth2 Authorization Code flow
- First successful backup: 92 playlists (83 with tracks, 9 skipped 403), 217 saved albums
- Top tracks endpoint returning 502 (Spotify instability) — gracefully handled, will work on retry
- Backup saved to `data/2026-03-10/backup.json` (17MB)
- Spotify API changes: endpoint renamed `/tracks` → `/items`, needs `market=BR` param, `user-read-private` scope
- Auth flow uses pager.jsplayground.cc tunnel (stop pager-api → port 3100 → auth → restart pager-api)
- Spotify credentials: app ID `a51170d21c2b469891afe76b9ae065f0`
- João's Spotify email added to User Management in developer dashboard

## TODOs
- Cleaned up TODOs (removed done/cancelled items from Segura, Personal)
- Added and completed Spotify backup tracker in TODO-Jonathan and TODO-Personal

## Issues During Build
- Claude Code kept asking for file approval (shift+tab didn't work for "allow all")
- Had to restart with `--dangerously-skip-permissions`
- Spotify Developer Dashboard was unstable (Cheddar errors)
- Multiple auth attempts needed due to server timeouts and redirect URI mismatches

## Diff/Notify System Built
- Claude Code created `src/diff.ts` (changelog detection), `src/notify.ts` (WhatsApp-friendly formatting)
- `src/index.ts` updated: finds previous backup, runs diff, saves `changelog.json`, prints notification
- `--notify` flag sends via `openclaw system event`
- `npm run backup:notify` script added to package.json

## Cron Configured
- Job: "Spotify Backup Semanal" (id: `85dd6e76-af9f-45c3-a0ea-89773426d239`)
- Schedule: Tuesdays 8:15 BRT, isolated session, delivers report via WhatsApp
- First run: 2026-03-17

## Test Run — Full Success
- Second backup run: top tracks WORKING (8,584 tracks across 3 ranges), 218 albums (+1 new: ? by XXXTENTACION)
- Simulated diff test: detected playlist removal + new album correctly
- Notification format confirmed working via `openclaw system event`

## Next Steps
- Terça 17/03: primeiro report automático real via cron

## Refinements (afternoon)
- João flagged all-time top tracks are buggy (Turkish songs that aren't his)
- Changed top tracks to only fetch `short_term` (últimas 4 semanas)
- Added `--top-tracks` flag to index.ts — top tracks only fetched when explicitly requested
- Split cron into 2 jobs:
  - "Spotify Backup Semanal" (days 8-31, Tuesdays 8:15) — playlists + albums diff only
  - "Spotify Top Tracks Mensal" (days 1-7, first Tuesday 8:15) — full backup + top 20 tracks
- Final test: full report sent to João, detected Ventura (Los Hermanos) removed from albums
- Correction: João doesn't like "tu" — usar "você" ou "ce"
